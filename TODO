Core
====
 * multiple assginment, a, b, c = sequence_object(list/tuple, etc)
 * turn lexer/parser error into Exception?
 * implement `this` as keyword or a special name?
 * implement module object, then implement gc module
 * implement gc on methodobject
 * separate subscript with getattr/setattr
 * prefer o:meth(arg1, arg2, ...) style, remove methodobject

Performance
===========
 * frameobject freelist

Tests
=====
  * unit tests with valgrind memory leak check

Bugs
====
