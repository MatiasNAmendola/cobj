Core
====
 * remote tuple literal syntax to simplify gramma
 * support one line call syntax, like this:
    print "hello world" # in one line
   which is same as 
    print("hello world")
 * rework closure
 * turn lexer/parser error into Exception?
 * hand-written lexer?
 * implement `this` as keyword or a special name?
 * coroutine

Performance
===========
 * frameobject freelist
 * float hash function

Tests
=====
  * unit tests with valgrind memory leak check
