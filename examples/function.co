#!./co

func fibo(num) {
    if (num == 0) {
        return 0;
    } else if (num == 1) {
        return 1;
    } else {
        return fibo(num - 1) + fibo(num - 2);
    }
}

print fibo(0);
#i = 0;
#while (i < 1) {
    #print fibo(i);
    #i = i + 1;
#}
